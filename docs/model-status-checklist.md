# Solartron-Plan 当前模型启动状态清单

更新时间：2026-03-16

## 说明

- `[x]` 已启用：当前代码路径已经参与运行
- `[ ]` 未启用：备注里会进一步区分“保留未启用”或“当前未提供”

## 当前结论

- 当前模型已经收缩到“先保证换线/换型逻辑可用”的状态。
- 真实 Excel 主链路可跑通，当前已验证结果为 `0hard/0medium/-3130soft`。
- 前端只保留一套源码，但目前只覆盖 demo JSON 求解，不覆盖真实 Excel 上传。

## 总体目标

| 状态 | 功能项 | 说明 |
| --- | --- | --- |
| `[x]` | 当前排程目标聚焦换线/换型可用 | 当前主链路是“分线 + 换型时间最小化” |
| `[x]` | 真实 Excel 端到端求解可行 | 当前已验证为硬约束可行 |

## 求解核心

| 状态 | 功能项 | 说明 |
| --- | --- | --- |
| `[x]` | Timefold Solver 同步求解 | 后端同步 `solve` 主链路已接通 |
| `[x]` | Timefold Solver 异步求解 | 支持 `jobId`、轮询状态和提前停止 |
| `[x]` | 按 Excel 原始订单粒度求解 | 当前预处理不再按天拆分订单 |
| `[x]` | 可行性标记 `isFeasible` 输出 | 接口响应已返回 `isFeasible` |
| `[ ]` | 按天拆分任务求解 | 保留未启用；`TaskSplitter` 和相关测试仍在，但当前 `preprocess` 未启用 |

## 约束

| 状态 | 功能项 | 说明 |
| --- | --- | --- |
| `[x]` | HC1 产线兼容约束 | 当前必须满足订单与产线兼容 |
| `[x]` | SC1 换型时间最小化 | 当前换线逻辑的核心软约束 |
| `[ ]` | HC2 库存 < 10 天优先 | 保留未启用；代码仍在，但 `defineConstraints` 未启用 |
| `[ ]` | HC3 停机 / ExceptionTime 避让 | 保留未启用；当前数据未装载，约束也未启用 |
| `[ ]` | HC4 厚度单峰 / 波峰约束 | 保留未启用 |
| `[ ]` | HC5 子任务保序 | 保留未启用；依赖拆分任务 |
| `[ ]` | MC1 过滤器后 20 天优先顺序 | 保留未启用 |
| `[ ]` | MC2 高库存不前移 | 保留未启用 |
| `[ ]` | SC2 库存天数优先级 | 保留未启用 |
| `[ ]` | SC3 期望开始时间偏差 | 保留未启用 |
| `[ ]` | SC4 特定产线偏好 | 保留未启用；`preferredLineCode` 数据会导入，但约束未启用 |
| `[ ]` | SC5 拆分子任务连续生产 | 保留未启用 |

## 数据导入

| 状态 | 功能项 | 说明 |
| --- | --- | --- |
| `[x]` | Excel 订单导入 | 真实 Excel 导入链路可用 |
| `[x]` | 换型矩阵构建 | 当前会构建换型矩阵并参与求解 |
| `[ ]` | 工厂日历对象 | 保留未启用；模型字段已保留，但当前装载为空日历 |
| `[ ]` | ExceptionTime 问题事实 | 保留未启用；当前 loader 传入空列表 |
| `[ ]` | FilterChangePlan 问题事实 | 保留未启用；当前 loader 传入空列表 |
| `[ ]` | 工艺时长 / 产能使用正式业务参数 | 当前未提供；现在仍有估算值，后续需替换为真实工艺数据 |

## 接口

| 状态 | 功能项 | 说明 |
| --- | --- | --- |
| `[x]` | JSON 同步求解 `/solve` | 可直接提交 `MotherRollSchedule` JSON |
| `[x]` | JSON 异步求解 `/solve-async` | 支持前端 demo 和接口轮询 |
| `[x]` | Excel 同步求解 `/solve-excel` | 支持上传 Excel 直接求解 |
| `[x]` | Excel 异步求解 `/solve-excel-async` | 支持上传 Excel 后异步求解 |
| `[x]` | 异步状态查询 `/status/{jobId}` | 支持轮询当前求解状态 |
| `[x]` | 提前停止 `/stop/{jobId}` | 支持终止异步求解任务 |
| `[x]` | 内置真实 Excel 演示 `/demo-real` | 便于不上传文件直接验证真实样例 |
| `[x]` | 假数据演示 `/demo` | 供前端 demo 页面使用 |

## 前端

| 状态 | 功能项 | 说明 |
| --- | --- | --- |
| `[x]` | 只保留一套前端源码 | 当前仓库只保留 `src/main/frontend` 这一套 |
| `[x]` | 前端同步求解按钮 | 当前页面可触发同步 demo 求解 |
| `[x]` | 前端异步求解按钮 | 当前页面可触发异步 demo 求解 |
| `[x]` | 前端异步轮询状态 | 当前页面会轮询 `/status/{jobId}` |
| `[ ]` | 前端真实 Excel 上传 UI | 当前未提供 |

## 验证

| 状态 | 功能项 | 说明 |
| --- | --- | --- |
| `[x]` | 单元 / 约束测试 | 当前测试聚焦已启用的兼容性和换型时间 |
| `[x]` | Excel 端到端集成测试 | 当前真实 Excel 流程已纳入自动化测试 |
| `[x]` | 当前真实 Excel 硬约束可行 | 最近一次验证结果为 `0hard/0medium/-3130soft` |
